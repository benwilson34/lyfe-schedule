// maybe these should be defined elsewhere?

export enum SortMode {
  Priority = "Priority",
  StartDate = "Start Date",
  EndDate = "End Date",
  RangeDays = "Range",
  TimeEstimate = "Time Estimate",
  RepeatInterval = "Repeat",
}
